def solution(board, h, w):
    drc = [(-1, 0), (0, -1), (1, 0), (0, 1)]
    cnt = 0
    for mh, mw in drc:
        if h+mh >= 0 and len(board)-1 >= h+mh and w+mw >= 0 and len(board[0])-1 >= w+mw and board[h+mh][w+mw] == board[h][w]: cnt += 1
    return cnt